'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { createSupabaseBrowserClient } from '@/lib/supabase/config';

export default function LoginPage() { const router=useRouter(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); async function submit(event:FormEvent){event.preventDefault(); setError(''); setLoading(true); const client=createSupabaseBrowserClient(); if(!client){setError('Integração Supabase não configurada.');setLoading(false);return;} const result=await client.auth.signInWithPassword({email,password}); if(result.error){setError('E-mail ou senha inválidos.');setLoading(false);return;} router.replace('/admin'); router.refresh();} return <main className="flex min-h-screen items-center justify-center px-6"><form onSubmit={submit} className="w-full max-w-md border border-white/10 bg-surface p-8"><p className="text-xs uppercase tracking-[0.35em] text-gold">Área administrativa</p><h1 className="mt-4 font-display text-4xl">Acesso seguro</h1><label className="mt-8 block text-sm text-zinc-400">E-mail<input required type="email" value={email} onChange={(e)=>setEmail(e.target.value)} className="mt-2 w-full border border-zinc-700 bg-ink px-4 py-3 text-white outline-none focus:border-gold" /></label><label className="mt-5 block text-sm text-zinc-400">Senha<input required type="password" value={password} onChange={(e)=>setPassword(e.target.value)} className="mt-2 w-full border border-zinc-700 bg-ink px-4 py-3 text-white outline-none focus:border-gold" /></label>{error&&<p role="alert" className="mt-4 text-sm text-red-300">{error}</p>}<button disabled={loading} className="mt-7 w-full bg-gold px-5 py-3 text-sm uppercase tracking-[0.15em] text-ink disabled:opacity-50">{loading?'Entrando…':'Entrar'}</button></form></main>; }
